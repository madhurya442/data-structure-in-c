#include<stdio.h>
#include<stdlib.h>
int main()
{
        int*p,i;
        p=(int*)calloc(3,sizeof(int));
        for(i=0;i<3;i++)
        {
                printf("%d",p[i]);
        }
        free(p);
        return 0;
}
