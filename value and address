#include<stdio.h>
int main()
{
        int a=10;
        int*ptr;
        ptr=&a;
        printf("value of a=%d\n",a);
        printf("Address of a=%p\n",&a);
        printf("Value stored in pointer=%p\n",ptr);
        printf("value pointed by pointer=%d\n",*ptr);
        return 0;
}
